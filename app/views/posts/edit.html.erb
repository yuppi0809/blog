    <div class="row below_the_fixed">

      <div class=""> <!-- Note that "m4 l3" was added -->
        <!-- Grey navigation panel

              This content will be:
          3-columns-wide on large screens,
          4-columns-wide on medium screens,
          12-columns-wide on small screens  -->
 <div class="form_background">

  <%= form_for @post, url: { action: "update"}, method: :patch do |f| %>
      <div style="margin: 8px 0">
            <%= f.text_area :title, value: @post.title,id: 'new_posts_title' %>
          </div>
          <div style="margin: 8px 0">
            <%= f.text_area :text, placeholder: '本文', value: @post.text,style: 'width: 100%;height: 300px;',id: 'new_posts' %>
          </div>
          <div class="">
            <div class="">
              <%= f.submit value: "編集する", id: "submit_btn"%>
              <i class="icon-arrow-right"></i>
              <% end %>

           </div>
      </div>
    </div>
  </body>
</html>
